import React from 'react';

function HeroButton() {
  return (
    <button className="btn bg-white rounded-pill hero-btn">
      <span className="hero-btn-text">I'm flexible</span>
    </button>
  );
}

export default HeroButton;
